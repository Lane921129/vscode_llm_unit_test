# VS Code LLM Unit Test Extension - Python Dependencies
# These are the complete Python runtime dependencies used by the extension.
# Install them into the workspace .venv; do not depend on global Python
# packages. Cloud Gemini and Ollama are called by the VS Code extension over
# HTTP and deliberately need no Python SDK.

# mutatest 3.x requires coverage < 6. Native mutmut does not support Windows;
# Windows/Python 3.12+ uses the built-in AST mutation runner; native mutmut needs WSL.
mutatest>=3.1.0; python_version < "3.12"
coverage>=4.4,<6; python_version < "3.12"
mutmut>=3.5.0; python_version >= "3.12" and platform_system != "Windows"
coverage>=7.0.0; python_version >= "3.12"
