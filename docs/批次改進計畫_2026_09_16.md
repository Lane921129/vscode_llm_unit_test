# 2026-09-16 未完成批次分析與改進計畫

## 分析界線

分析批次 `all_test_2026_09_16_21_11`，以 2026-09-16 22:43（Asia/Taipei）讀取的部分結果為基準。
原實驗尚未完成；檔案的 running 狀態只表示最後檢查點，不證明程序仍存活。保持原結果與原應用不變，不重跑原應用或存取共享資料庫。
詳細統計保存於本機 output 分析目錄，不將私有來源、完整模型回覆或憑證納入 Git。

## 已確認的證據

- 429 份報告：127 筆有一般執行 manifest，另 302 筆後續逐份確認均有明確 Dummy 略過標記，不能計為成功。
- 127 筆紀錄：115 failed、3 retained-after-failure、6 passed、2 stub-smoke-generated、1 running。
- 六筆 passed 均有 Reviewer 未完成警告；它們只是工具通過，不能宣稱完整品質驗收通過。
- 81 筆在模組載入的 Path.mkdir 副作用阻擋處停止。這是環境／應用邊界，不能解除安全 gate；需減少同模組重複的前置工作。
- 10 筆獨立 Trace baseline 失敗：三筆為底線開頭目標未經 wildcard import 匯入，七筆為 OperationalError 未定義。後者 Trace 訊息為「unable to open database file」，暴露 SQLite 連線沒有被 Trace 副作用隔離攔下，不能只補 exception import 就視為修好。
- 35 次 Reviewer invalid-response；這些「結構通過至審查回傳」區間合計約 511.92 秒。另有 27 次可解析審查。上述區間包含等待與請求，不能當作純模型運算時間。
- 59 次 writer/tier-failed、13 次 Writer repeated、4 次 Bug Fixer repeated，顯示相同候選與契約問題反覆消耗請求。
- 生成候選包含標準 mock.assert_called_once_with／assert_has_calls，卻仍遇到缺 assertion gate；需以可證明與 target 使用點關聯的 AST 證據判定，不能直接放行所有同名 assert 方法。
- 127 筆均沒有分角色資格資料；手動 Tier 可執行，但不能據此宣稱角色 verified。Auto 舊 Writer 資格不可替代 Reviewer／Bug Fixer 資格。

## 實作順序與驗收

1. **Trace 產物與 SQLite 隔離（P0）**
   - 明確匯入所選目標，涵蓋底線命名與 __all__。
   - Trace／模組預檢阻擋檔案型、共享 URI SQLite；只接受真正獨立的 in-memory 資源。
   - 可產生的例外斷言必須有可解析且已驗證的型別身分；未知型別不能退成廣義 Exception。
   - 中性 fixture 真實驗證 Trace、獨立 unittest 與安全阻擋，確認原始檔案／外部資料沒有被變更。

2. **預檢順序與快取生命週期（P1）**
   - 正規模組預檢移到深度相依 Trace／呼叫點探索之前，仍先保存 running 與診斷。
   - 快取以一次分析／批次為範圍；新分析重新檢查修好的環境，同批次相同障礙去重，保留匯入根優先順序。
   - 以缺相依 → 重複目標 → 補依賴 → 新分析成功驗證；取消／暫時工具失敗不可永久變成環境失敗。

3. **Mock 行為斷言的正確辨識（P1）**
   - 接受標準 mock 呼叫斷言，但必須證明 mock 綁定於目標的外部使用點或傳入目標，且同一可執行測試有 target call。
   - 拒絕未使用 mock、偽裝同名方法、字串／註解中的 assertion、錯 patch 路徑，以及真實 I/O。
   - 以可執行的資料庫替身／外部邊界中性案例及對應反例驗證，不放寬 existing gate。

4. **重複審查、角色資格與完成狀態（P1）**
   - 對相同來源／上下文／候選避免重做相同審查；對持續契約不合格的 Reviewer 採有界、單次分析範圍的停止機制，明示未完成。
   - 保留真正有證據的 blocking；不把未知審查當作空問題通過。
   - Auto 要求各角色自身的有效資格，Writer 修訂使用 Writer 的資格。
   - 機讀狀態與 scorecard 區分「工具通過、審查未完成」、stub 未執行及完整通過。

5. **驗證、交付與實驗室復測**
   - 每個行為改動有回歸；完整 Node、Python、型別、lint、建置、Webview、密鑰及差異檢查。
   - 與中性 fixture 的請求／程序次數比較佐證效率，不由模型名稱推定速度，也不杜撰原批次改善率。
   - 更新 CHANGELOG.zh-TW.md 與必要架構文件，建立中英 commit 後 push 至既有 upstream。
   - 實驗室 pull 後先跑五類樣本，保存四份執行證據與指定模型／生成模式的 scorecard；實際生成成功率與時間改善待該輪量測。

## 實作與驗收進度

- 1–4 項已實作並補回歸；包含實際 Python 基線、SQLite 安全阻擋、相依修復後新分析、可執行 mock 測試及完整 coverage／mutation 搭配有效與無效 Reviewer 的整合案例。
- 效率驗收以確定性的程序／角色請求數進行：同批次兩次相同環境失敗為一個預檢程序；新批次另起預檢。四次候選審查需求（包含重複候選與後續候選）至多兩次連續失敗請求，其餘事件標示 cache-hit／suspended。
- 本機完整驗證：Node 300、Python 113 通過；型別、lint、建置、Webview、密鑰掃描通過。提交後由實驗室先跑五類小批次；既有環境 Path.mkdir 副作用仍須處理為可匯入的應用邊界，不會為提高成功數解除阻擋。
